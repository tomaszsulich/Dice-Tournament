# Manual API

[Polski](README.md) | **English**

This directory contains HTTP requests for manually testing the Dice Tournament API.

Requests will be added and maintained alongside the API endpoints they exercise.

Use `http-client.env.json.example` as a template for local environment configuration.
<br>Real credentials, passwords and tokens must never be committed to the repository.
